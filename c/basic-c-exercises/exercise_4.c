// lenght and height of a rectangle and perimeter

#include <stdio.h>
int main () {
    int length;
    printf("Enter length of the rectangle: ");
    scanf("%d", &length);
    printf("The length of the rectangle is %d", length);
}