var numerosZeroAteCem = 0;
for (var index = 0; index <= 100; index++){
    numerosZeroAteCem += index;
    for (var linha = 0; linha <= 100; linha++){
    }
console.log(index);
}
// imprime todos os números de 0 até 100 em cada linha de 0 até 100;