var word = "APLLE";
var reverseWord = "";
for (var index = word.length - 1; index >= 0; index--) { 
reverseWord += word[index]
}
console.log(reverseWord);
// escreve a string (word) ao contrário Ex: APPLE retorna ELPPA