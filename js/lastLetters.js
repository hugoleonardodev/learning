function lastLetters(word) {
    var lastLetter = word[(length)];
    var secondLastLetter = word[(length-1)];
    var lastLetters = lastLetter + " " + secondLastLetter  
    return lastLetters
    }
    // função que retorna as duas ultimas letras de uma string em ordem invertida com espaço entre elas