var word = "APPLE";
var lastLetter = word[(word.length-1)];
var secondLastLetter = word[(word.length-2)];
var twolastLetters = lastLetter + " " + secondLastLetter;
console.log(twolastLetters);
// função que retorna as duas ultimas letras de uma string em ordem invertida com espaço entre elas
